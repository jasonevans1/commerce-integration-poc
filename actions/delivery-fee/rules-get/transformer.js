/**
 * Pass-through transformer for rule data.
 *
 * @param {object|null} rule - Rule object or null
 * @returns {object|null} Rule unchanged
 */
function transformData(rule) {
  return rule;
}

module.exports = { transformData };
