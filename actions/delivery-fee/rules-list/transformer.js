/**
 * Transforms the rules array for the response.
 * Pass-through — no transformation required.
 *
 * @param {object[]} rules - Array of rule objects
 * @returns {object[]} rules unchanged
 */
function transformData(rules) {
  return rules;
}

module.exports = { transformData };
